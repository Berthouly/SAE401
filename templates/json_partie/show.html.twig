{% extends 'base.html.twig' %}

{% block title %}JsonPartie{% endblock %}

{% block body %}
    <h1>JsonPartie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ json_partie.id }}</td>
            </tr>
            <tr>
                <th>Nom_fichier</th>
                <td>{{ json_partie.nomFichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_json_partie_index') }}">back to list</a>

    <a href="{{ path('app_json_partie_edit', {'id': json_partie.id}) }}">edit</a>

    {{ include('json_partie/_delete_form.html.twig') }}
{% endblock %}
